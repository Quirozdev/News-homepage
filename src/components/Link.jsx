const Link = ({ text }) => {
  return (
    <a
      href="#"
      className="text-xl hover:cursor-pointer hover:text-soft-orange text-very-dark-blue"
    >
      {text}
    </a>
  );
};

export default Link;
